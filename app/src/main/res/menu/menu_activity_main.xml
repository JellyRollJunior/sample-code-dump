<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <!-- "Mark Favorite", should appear as action button if possible -->
    <item
        android:id="@+id/action_run_fast_sample_twelve"
        android:icon="@drawable/ic_baseline_directions_run_24"
        android:title="@string/action_bar_sample_twelve"
        app:showAsAction="always"/>

    <!-- Settings, should always be in the overflow -->
    <item android:id="@+id/action_settings"
        android:title="@string/action_bar_options_sample_twelve"
        app:showAsAction="ifRoom"/> <!-- can also be "never" -->

</menu>